{
  "name": "shopback-bot",
  "version": "0.0.1",
  "description": "Follow shopback offers",
  "scripts": {
    "dev": "ts-node --transpile-only src/index.ts",
    "build": "tsc && ncc -m build out/index.js && rm -rf out",
    "clean": "rm -rf dist out"
  },
  "author": "hyperbola",
  "packageManager": "pnpm@7.13.4",
  "devDependencies": {
    "@types/node": "^16.11.65",
    "@vercel/ncc": "^0.34.0",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "axios": "^1.1.3",
    "commander": "^9.4.1",
    "ts-custom-error": "^3.2.2"
  }
}
